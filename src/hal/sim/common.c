#include "../common.h"

void register_interrupt_handler(
    const char number,
    const interrupt_handler_t* handler
) {
    // do nothing
}

void delay_ms(const unsigned int ms) {
    // do nothing
}
